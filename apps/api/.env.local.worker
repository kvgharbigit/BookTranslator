#!/bin/bash
# Source this before running the worker locally
# Usage: source .env.local.worker

# Fix macOS fork() issue with WeasyPrint
export OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES

# Load environment from .env.local
set -a
source .env.local
set +a

echo "✅ Worker environment loaded (using .env.local)"
